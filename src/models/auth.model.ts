export interface AuthModel {
    email: string;
    password: string;
}

export interface RegistrationModel {
    first_name: string;
    last_name: string;
    password: string;
    phone: string;
    email: string;
}