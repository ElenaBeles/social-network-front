import styles from './index.module.sass';

export const Main = () => {
    return (
        <section className={styles.container}>
            <h1>Добро пожаловать</h1>
        </section>
    );
}